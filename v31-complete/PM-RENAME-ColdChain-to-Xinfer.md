═══════════════════════════════════════════════════════════════════════════
🏗️  AHS PROMPT MAESTRO — Cell Rename: ColdChain → Xinfer
Version: 1.0 | Blueprint: V3.1.1 | Generated by: C2
Type: Refactor — namespace rename only, zero logic changes
═══════════════════════════════════════════════════════════════════════════

## SECTION 0 — CONTEXT & CONSTRAINTS

You are AG, the AHS Executor. This is a PURE RENAME operation.

CRITICAL RULES — READ BEFORE TOUCHING ANY FILE:
1. DO NOT change any business logic, algorithms, or domain rules
2. DO NOT change any CSS, UI markup, or component structure
3. DO NOT change variable names INSIDE methods (only namespaces/types/files)
4. DO NOT change: XaiDiagnostic, XaiRiskMonitor, XaiDna — these already
   have the correct X prefix and stay exactly as-is
5. ONLY change: namespace declarations, project names, folder names,
   file names, using statements, csproj references, slnx entries,
   Dockerfile paths, and GitHub Actions paths

PRONUNCIATION NOTE (add as comment in Program.cs):
// AHS Xinfer — pronounced "ex-infer" — eXplainable Inference for Logistics Risk

---

## SECTION 1 — RENAME MAP

### Project names (rename .csproj files and folders)

```
OLD NAME                                    NEW NAME
─────────────────────────────────────────── ─────────────────────────────
AHS.Cell.ColdChain.Domain                → AHS.Cell.Xinfer.Domain
AHS.Cell.ColdChain.Application           → AHS.Cell.Xinfer.Application
AHS.Cell.ColdChain.Infrastructure        → AHS.Cell.Xinfer.Infrastructure
AHS.Cell.ColdChain.Contracts             → AHS.Cell.Xinfer.Contracts
AHS.Cell.ColdChain.API                   → AHS.Cell.Xinfer.API
AHS.Cell.ColdChain.Tests                 → AHS.Cell.Xinfer.Tests
```

### Folder paths (rename physical folders)

```
OLD PATH                                    NEW PATH
─────────────────────────────────────────── ─────────────────────────────
src/Cells/ColdChain/                     → src/Cells/Xinfer/
tests/Cells/ColdChain/                   → tests/Cells/Xinfer/
```

### Namespace declarations (global search and replace)

```
OLD NAMESPACE                               NEW NAMESPACE
─────────────────────────────────────────── ─────────────────────────────
namespace AHS.Cell.ColdChain.Domain      → namespace AHS.Cell.Xinfer.Domain
namespace AHS.Cell.ColdChain.Application → namespace AHS.Cell.Xinfer.Application
namespace AHS.Cell.ColdChain.Infrastructure → namespace AHS.Cell.Xinfer.Infrastructure
namespace AHS.Cell.ColdChain.Contracts   → namespace AHS.Cell.Xinfer.Contracts
namespace AHS.Cell.ColdChain.API         → namespace AHS.Cell.Xinfer.API
```

### Using statements (global search and replace in all .cs and .razor files)

```
OLD USING                                   NEW USING
─────────────────────────────────────────── ─────────────────────────────
using AHS.Cell.ColdChain.Domain          → using AHS.Cell.Xinfer.Domain
using AHS.Cell.ColdChain.Application     → using AHS.Cell.Xinfer.Application
using AHS.Cell.ColdChain.Infrastructure  → using AHS.Cell.Xinfer.Infrastructure
using AHS.Cell.ColdChain.Contracts       → using AHS.Cell.Xinfer.Contracts
using AHS.Cell.ColdChain.API             → using AHS.Cell.Xinfer.API
```

### Solution file (AHS.SaaS.slnx)

```
Replace all occurrences of:
  ColdChain → Xinfer
  (in project paths and Solution Folder references)

Solution Folder in slnx:
  OLD: 📁 Cells / ColdChain
  NEW: 📁 Cells / Xinfer
```

### Dockerfile

```
OLD PATH REFERENCES:
  AHS.Cell.ColdChain.API/AHS.Cell.ColdChain.API.csproj
  ENTRYPOINT ["/app/AHS.Cell.ColdChain.API"]

NEW PATH REFERENCES:
  AHS.Cell.Xinfer.API/AHS.Cell.Xinfer.API.csproj
  ENTRYPOINT ["/app/AHS.Cell.Xinfer.API"]

Dockerfile location moves:
  OLD: src/Cells/ColdChain/Dockerfile
  NEW: src/Cells/Xinfer/Dockerfile
```

### GitHub Actions workflow (if exists)

```
Replace in .github/workflows/*.yml:
  AHS.Cell.ColdChain  → AHS.Cell.Xinfer
  src/Cells/ColdChain → src/Cells/Xinfer
  tests/Cells/ColdChain → tests/Cells/Xinfer
```

### Program.cs comment (ADD — do not replace anything else)

```csharp
// AHS Xinfer — eXplainable Inference for Logistics Risk
// Pronounced: "ex-infer"
// Namespace: AHS.Cell.Xinfer
// Replaces: AHS.Cell.ColdChain (renamed 2026-03)
```

### Service Bus topics (if configured)

```
OLD TOPIC:  ahs.coldchain.events
NEW TOPIC:  ahs.xinfer.events

Update in:
  - Infrastructure/ServiceBus/XinferCellEventPublisher.cs
  - docker-compose.yml (servicebus-config.json topic names)
  - Any appsettings.json with topic names
```

### Class names with "ColdChain" in the name (rename the class, not the concept)

```
OLD CLASS NAME                              NEW CLASS NAME
─────────────────────────────────────────── ─────────────────────────────
ColdChainDbContext                       → XinferDbContext
ColdChainEventStore                      → XinferEventStore
ColdChainCellEventPublisher              → XinferCellEventPublisher
ColdChainServiceExtensions               → XinferServiceExtensions
ColdChainWebAppFactory (tests)           → XinferWebAppFactory
ColdChainJsonContext                     → XinferJsonContext
ColdChainArchitectureTests               → XinferArchitectureTests
ColdChainDashboard.razor                 → XinferDashboard.razor
```

### DO NOT RENAME — preserve exactly as-is

```
✅ XaiDiagnostic.razor       — already correct (Xai prefix)
✅ XaiRiskMonitor.razor       — already correct (Xai prefix)
✅ XaiDna                     — already correct
✅ ShipmentRiskProfile        — domain concept, not Cell name
✅ LogisticsOracle            — domain concept, not Cell name
✅ PredictiveShieldMetrics    — domain concept, not Cell name
✅ AuditEvent                 — domain concept, not Cell name
✅ ThermalDataPoint           — domain concept, not Cell name
✅ MeanKineticTemperature     — domain concept, not Cell name
✅ All domain events          — ShipmentCreated, ExcursionDetected, etc.
✅ All value objects          — CargoType, InsulationType, etc.
✅ AHS.Common.*               — foundation project, unchanged
✅ AHS.Web.Common.*           — foundation project, unchanged
✅ AHS.Web.UI.*               — presentation project, unchanged
```

---

## SECTION 2 — EXECUTION ORDER

```
1. [ ] Rename physical folders:
       src/Cells/ColdChain/ → src/Cells/Xinfer/
       tests/Cells/ColdChain/ → tests/Cells/Xinfer/

2. [ ] Rename .csproj files inside new folders (6 files)

3. [ ] Global search+replace namespaces in all .cs files

4. [ ] Global search+replace using statements in all .cs and .razor files

5. [ ] Rename class names with "ColdChain" in their name (8 classes)

6. [ ] Update AHS.SaaS.slnx (project paths + Solution Folder name)

7. [ ] Update Dockerfile paths and ENTRYPOINT

8. [ ] Update GitHub Actions workflow paths (if exists)

9. [ ] Update Service Bus topic names (ahs.coldchain → ahs.xinfer)

10. [ ] Add pronunciation comment to Program.cs

11. [ ] Update docker-compose.yml topic references
```

---

## SECTION 3 — QUALITY GATES

```
□ dotnet build AHS.SaaS.slnx → 0 errors
  (all 9 projects build: Foundation x2 + Xinfer x6 + Web.UI)

□ grep -r "ColdChain" src/Cells/ → 0 results
  (no remaining ColdChain references in Cell folder)

□ grep -r "ColdChain" tests/Cells/ → 0 results

□ grep -r "ahs.coldchain" . → 0 results
  (Service Bus topics updated)

□ grep -r "XaiDiagnostic\|XaiRiskMonitor\|XaiDna" . → results still exist
  (these were NOT renamed — verify they are intact)

□ dotnet test tests/Cells/Xinfer/AHS.Cell.Xinfer.Tests/ → all green
  (same tests pass with new namespace)

□ /coldchain/dashboard route → still works
  (page route string unchanged — only namespace changed)

□ git diff --stat → only renames and namespace string replacements
  (no logic changes in the diff)
```

═══════════════════════════════════════════════════════════════════════════
END OF PROMPT MAESTRO — Rename ColdChain → Xinfer
Zero logic changes — pure namespace refactor
After completion: update AHS-SKILLS-DISTRIBUTION-MAP.md
═══════════════════════════════════════════════════════════════════════════
