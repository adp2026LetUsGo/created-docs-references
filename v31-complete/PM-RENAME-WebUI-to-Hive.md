═══════════════════════════════════════════════════════════════════════════
🏗️  AHS PROMPT MAESTRO — Rename: AHS.Web.UI → AHS.Web.Hive
Version: 1.0 | Blueprint: V3.1.2 | Generated by: C2
Type: Refactor — namespace rename only, zero logic changes
Run AFTER: PM-RENAME-ColdChain-to-Xinfer.md is complete
═══════════════════════════════════════════════════════════════════════════

## SECTION 0 — CONTEXT & CONSTRAINTS

PURE RENAME. Zero logic changes.
AHS Hive is the shell that orchestrates all Cells.
"The hive functions even if individual cells die." — scale-to-zero.

RULES:
1. DO NOT change any business logic, CSS, or component markup
2. DO NOT change AHS.Common or AHS.Web.Common — foundation unchanged
3. DO NOT change Xinfer namespaces (renamed in previous PM)
4. ONLY change: project name, folder, namespaces, usings, slnx, paths

---

## SECTION 1 — RENAME MAP

### Project and folder

```
OLD                          NEW
────────────────────────     ────────────────────────
AHS.Web.UI                → AHS.Web.Hive
src/Presentation/AHS.Web.UI → src/Presentation/AHS.Web.Hive
AHS.Web.UI.csproj         → AHS.Web.Hive.csproj
```

### Namespaces (global search+replace)

```
namespace AHS.Web.UI      → namespace AHS.Web.Hive
using AHS.Web.UI          → using AHS.Web.Hive
@using AHS.Web.UI         → @using AHS.Web.Hive
```

### Class names with "UI" suffix that refer to this project

```
Only rename classes explicitly scoped to AHS.Web.UI project.
Do NOT rename generic UI classes or AHS.Web.Common classes.
```

### Solution file AHS.SaaS.slnx

```
Replace: AHS.Web.UI → AHS.Web.Hive
Solution Folder:
  OLD: 📁 Control Tower / AHS.Web.UI
  NEW: 📁 Control Tower / AHS.Web.Hive
```

### Dockerfile (if exists for Web.UI)

```
OLD: src/Presentation/AHS.Web.UI/AHS.Web.UI.csproj
NEW: src/Presentation/AHS.Web.Hive/AHS.Web.Hive.csproj
ENTRYPOINT: /app/AHS.Web.Hive
```

### Layout and brand update (AhsCommandLayout.razor)

```
Find the brand-sub element and update:
  OLD: "UNIVERSAL CONTROL TOWER" or "AHS CONTROL TOWER"
  NEW: "HIVE — ONE ORGANISM. INFINITE CELLS."

Find the brand-name element:
  OLD: "AHS" or "AHS.SaaS"  
  NEW: "AHS HIVE"

Add to Program.cs as comment:
  // AHS Hive — The shell that orchestrates all Cells
  // "The hive functions even if individual cells die."
  // Namespace: AHS.Web.Hive | Blueprint V3.1.2
```

### GitHub Actions (if exists)

```
Replace: AHS.Web.UI → AHS.Web.Hive
Replace: src/Presentation/AHS.Web.UI → src/Presentation/AHS.Web.Hive
```

---

## SECTION 2 — EXECUTION ORDER

```
1. [ ] Rename folder: src/Presentation/AHS.Web.UI → AHS.Web.Hive
2. [ ] Rename AHS.Web.UI.csproj → AHS.Web.Hive.csproj
3. [ ] Global search+replace namespaces in all .cs and .razor files
4. [ ] Update AHS.SaaS.slnx
5. [ ] Update Dockerfile paths
6. [ ] Update GitHub Actions
7. [ ] Update brand text in AhsCommandLayout.razor
8. [ ] Add comment to Program.cs
```

---

## SECTION 3 — QUALITY GATES

```
□ dotnet build AHS.SaaS.slnx → 0 errors (all projects)
□ grep -r "AHS\.Web\.UI" src/ → 0 results
□ grep -r "AHS\.Web\.Hive" src/ → results exist (renamed correctly)
□ grep -r "AHS\.Web\.Common" src/ → results exist (NOT renamed)
□ grep -r "AHS\.Cell\.Xinfer" src/ → results exist (previous rename intact)
□ /coldchain/dashboard route → still works (routes unchanged)
□ Brand shows "AHS HIVE" in sidebar
□ git diff --stat → only renames and string replacements
```

═══════════════════════════════════════════════════════════════════════════
END OF PROMPT MAESTRO — Rename AHS.Web.UI → AHS.Web.Hive
Execution order:
  1. PM-RENAME-ColdChain-to-Xinfer.md  (run first)
  2. PM-RENAME-WebUI-to-Hive.md        (run second — this file)
After both complete: verify full solution builds in 0 errors
═══════════════════════════════════════════════════════════════════════════
